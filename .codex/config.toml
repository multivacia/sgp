# Codex project configuration — SGP+ Web
# Mantemos este arquivo pequeno. As regras principais ficam em AGENTS.md.

[agents]
max_threads = 5
max_depth = 1
job_max_runtime_seconds = 1800

# Segurança: não configurar danger-full-access aqui.
# O modo de sandbox principal fica sob controle do usuário/sessão.
# Cada custom agent define seu sandbox quando necessário.
